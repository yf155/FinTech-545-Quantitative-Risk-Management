1. Install the packages which are used in the code.
    Make sure that the version of seaborn is at least 0.11.0
2. For .jl and .py file, open them in vscode and run them. 
    For .ipynb, open jupyter notebook or google colab and run the blocks.
